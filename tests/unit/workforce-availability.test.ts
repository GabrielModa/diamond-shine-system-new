import { describe, expect, it } from 'vitest'
import { capacityBand, remainingCapacityMinutes, resolveWorkforceContext } from '../../src/lib/workforce-availability'
const base={timezone:'Europe/Dublin',home:{kind:'home' as const,label:'Home',address:'Clontarf',latitude:53.36,longitude:-6.19},school:{kind:'school' as const,label:'Trinity',address:'Dublin 2',latitude:53.34,longitude:-6.25},studySchedule:[{dayOfWeek:1,startsMinute:540,endsMinute:1020}],leaves:[]}
describe('workforce context',()=>{it('uses school in study window',()=>expect(resolveWorkforceContext(base,new Date('2026-08-24T13:00:00Z')).state).toBe('school'));it('school holiday falls back home',()=>expect(resolveWorkforceContext({...base,leaves:[{kind:'school_holiday' as const,startsAt:'2026-08-24T00:00:00Z',endsAt:'2026-08-25T00:00:00Z'}]},new Date('2026-08-24T13:00:00Z')).state).toBe('home'));it('personal leave hides availability',()=>expect(resolveWorkforceContext({...base,leaves:[{kind:'personal_leave' as const,startsAt:'2026-08-24T00:00:00Z',endsAt:'2026-08-25T00:00:00Z'}]},new Date('2026-08-24T13:00:00Z')).availableForScheduling).toBe(false))})
describe('capacity',()=>{it('bands correctly',()=>{expect(capacityBand(9*60)).toBe('0-10');expect(capacityBand(30*60)).toBe('30+')});it('does not return negative remaining capacity',()=>expect(remainingCapacityMinutes(1800,1900)).toBe(0))})
